// gather data from form
function getCityName() {
  const cityName = document.getElementById('city-name-input').value;
  return cityName;
}

// Add a function to let the user select the units from imperial or metric.
// function getUnits() {
//     let units =  const cityName = document.getElementById('#city-name-input').value;

//   }

// export default getCityName;
